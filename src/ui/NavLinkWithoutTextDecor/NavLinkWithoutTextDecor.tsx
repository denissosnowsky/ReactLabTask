import { NavLink } from "react-router-dom";
import styled from "styled-components";

export const NavLinkWithoutTextDecor = styled(NavLink)`
  text-decoration: none;
  color: inherit;
  font-weight: inherit;
  font-size: inherit;
`;
